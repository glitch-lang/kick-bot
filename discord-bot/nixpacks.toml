[phases.setup]
nixPkgs = ["nodejs_20", "python3"]
# Note: Chromium removed to speed up builds. Puppeteer features will be disabled.

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
