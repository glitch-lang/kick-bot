[phases.setup]
nixPkgs = ["nodejs_20", "python3"]
# Optimized for Railway: Removed Chromium to speed up builds

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
